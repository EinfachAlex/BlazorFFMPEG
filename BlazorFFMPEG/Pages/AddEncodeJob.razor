@page "/addEncodeJob"
<PageTitle>Add encode job</PageTitle>

@using BlazorFFMPEG.Components
@using BlazorFFMPEG.Shared.DTO

<CodecSelectionDropDown></CodecSelectionDropDown>
<br/>
<QualityMethodSelectionDropDown @bind-selectedQualityMethod="selectedQualityMethod"></QualityMethodSelectionDropDown>
<QualityValueSliderRange @bind-selectedQualityMethod="selectedQualityMethod"></QualityValueSliderRange>

<br/>
<input @bind="filePath" style="margin: 10px"/>

<button class="btn btn-primary" >Start Encode</button>

@code
{
    [Parameter]
    public AvailableQualityMethod? selectedQualityMethod { get; set; }
    private string filePath = "autistenkicker";
}